# Python dependencies for VitPose inference (HuggingFace transformers)
# The Deno server auto-installs missing packages on startup.
# Manual install: pip install -r py-requirements.txt

torch>=2.3.0
torchvision>=0.18.0
transformers>=4.48.0
numpy>=1.24.0
Pillow>=10.0.0
supervision>=0.18.0

# Inference model training (python/f_o_train_inference.py) — lightweight, CPU-only
scikit-learn>=1.3.0
joblib>=1.3.0
