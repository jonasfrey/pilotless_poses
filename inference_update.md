also in the pose preview , failed images show. by default they should not show. (add a toggle button to show failed images) 


the next feature is a inference model page. a model has CRUD . a model will need two paths to folders. one for positive example and one for negative. in the folder there are json files with the information of an image. then there should be a way to train a model with the data. so the target is to create a ai model that learns from the selected data. in the preview page then there will be another column 'inference models' there the user will be able to filter the images with a trained inference model. so basically the inference model page will train a model for a binary classification problem. i already have a idea how to solve this (the first part filtering the images we do not need since we already have the possibility of applying processor filters and storing the images into folders ) , for the idea see inference_idea.md


